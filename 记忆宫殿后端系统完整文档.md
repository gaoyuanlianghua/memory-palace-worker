# 记忆宫殿后端系统完整文档

> **系统名称**: 记忆宫殿分身系统 - 后端Worker  
> **版本**: MemoryChain/1.0 v4.10.1  
> **运行环境**: Cloudflare Worker + D1 Database  
> **API基础地址**: https://gyuanpalace.xyz/api  
> **最后更新**: 2026-08-17  

---

## 目录

1. [系统架构概览](#1-系统架构概览)
2. [数据库设计](#2-数据库设计)
3. [核心模块](#3-核心模块)
4. [API端点详解](#4-api端点详解)
5. [经济系统](#5-经济系统)
6. [分身与克隆系统](#6-分身与克隆系统)
7. [记忆链系统](#7-记忆链系统)
8. [治理系统](#8-治理系统)
9. [定时任务系统](#9-定时任务系统)
10. [安全机制](#10-安全机制)

---

## 1. 系统架构概览

### 1.1 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 运行环境 | Cloudflare Worker | 无服务器边缘计算 |
| 数据库 | Cloudflare D1 | SQLite兼容的边缘数据库 |
| 认证方式 | HMAC-SHA256 | 基于钱包的签名验证 |
| 前端托管 | Cloudflare Pages | 静态资源托管 |
| 路由分发 | Worker → Pages | API走Worker，页面走Pages |

### 1.2 请求路由

```
用户请求 → gyuanpalace.xyz
                ↓
        Worker (gyuanpalace)
                ↓
    ┌──────────┴──────────┐
    ↓                     ↓
/api/* 请求          非API请求
    ↓                     ↓
Worker直接处理      转发到Pages前端
(D1数据库操作)     (React应用)
```

### 1.3 核心常量

```javascript
var PROTOCOL = "MemoryChain/1.0";
var VERSION = "4.10.1";
```

---

## 2. 数据库设计

### 2.1 数据库表清单

| # | 表名 | 用途 |
|---|------|------|
| 1 | wallets | 钱包信息 |
| 2 | wallet_credentials | 钱包凭证 |
| 3 | agents | 代理/分身信息 |
| 4 | rooms | 房间/领域 |
| 5 | tasks | 任务系统 |
| 6 | memory_chains | 记忆链 |
| 7 | memory_chain_backups | 记忆链备份 |
| 8 | semantic_concepts | 语义概念 |
| 9 | semantic_edges | 语义关系边 |
| 10 | semantic_contexts | 语义上下文 |
| 11 | clone_identities | 克隆身份 |
| 12 | identity_bindings | 身份绑定 |
| 13 | auth_sessions | 认证会话 |
| 14 | audit_log | 审计日志 |
| 15 | events | 事件日志 |
| 16 | commands | 命令队列 |
| 17 | system | 系统配置 |
| 18 | strategies | 策略参数 |
| 19 | debts | 债务系统 |
| 20 | proposals | 治理提案 |
| 21 | votes | 投票记录 |
| 22 | bug_reports | Bug报告 |
| 23 | workflow_jobs | 工作流任务 |
| 24 | hooks | 钩子注册 |
| 25 | miners | 矿工注册 |
| 26 | wallet_nodes | 钱包节点 |
| 27 | node_connections | 节点连接 |
| 28 | monitoring_data | 监控数据 |
| 29 | pool_expansion | 池扩展记录 |
| 30 | pool_transactions | 池交易记录 |
| 31 | worker_code | Worker代码版本 |
| 32 | worker_deploy_log | 部署日志 |
| 33 | worker_secrets | Worker密钥 |

### 2.2 核心表结构

#### wallets（钱包表）

| 字段 | 类型 | 说明 |
|------|------|------|
| wallet | TEXT PK | 钱包地址 |
| balance | REAL | 余额（MC代币） |
| agent_id | TEXT | 绑定的代理ID |
| registered_at | INTEGER | 注册时间 |

#### agents（代理/分身表）

| 字段 | 类型 | 说明 |
|------|------|------|
| agent_id | TEXT PK | 代理唯一ID |
| wallet | TEXT | 绑定的钱包 |
| clone_id | TEXT | 克隆ID |
| rooms | TEXT | 关联的房间列表 |
| main_data | TEXT | 主体数据（JSON） |
| clone_data | TEXT | 克隆数据（JSON） |
| main_status | TEXT | 主体状态 |
| clone_status | TEXT | 克隆状态 |
| status | TEXT | 整体状态 |
| balance | REAL | 余额 |
| experience | INTEGER | 经验值 |
| resurrection_level | INTEGER | 复活等级 |
| created | INTEGER | 创建时间 |
| last_sync | INTEGER | 最后同步时间 |

#### memory_chains（记忆链表）

| 字段 | 类型 | 说明 |
|------|------|------|
| chain_id | TEXT PK | 链ID |
| agent_id | TEXT | 代理ID |
| wallet | TEXT | 钱包地址 |
| chain_data | TEXT | 链数据（JSON） |
| chain_length | INTEGER | 链长度 |
| compressed_bytes | INTEGER | 压缩后字节 |
| original_bytes | INTEGER | 原始字节 |
| level | INTEGER | 等级 |
| checksum | TEXT | 校验和 |
| backup_path | TEXT | 备份路径 |
| created | INTEGER | 创建时间 |
| updated | INTEGER | 更新时间 |

#### tasks（任务表）

| 字段 | 类型 | 说明 |
|------|------|------|
| task_id | TEXT PK | 任务ID |
| title | TEXT | 任务标题 |
| description | TEXT | 任务描述 |
| reward | REAL | 奖励（MC） |
| room | TEXT | 所属房间 |
| required_level | INTEGER | 所需等级 |
| claimed_by TEXT | 认领者 |
| completed_by | TEXT | 完成者 |
| result | TEXT | 结果 |
| status | TEXT | 状态 |
| created | INTEGER | 创建时间 |
| deadline | INTEGER | 截止时间 |

---

## 3. 核心模块

### 3.1 钱包认证模块

#### 认证流程

```
1. 注册钱包 → /api/wallet/register
2. 获取挑战码 → /api/wallet/challenge
3. 签名验证 → /api/wallet/verify
4. 获取会话Token → 后续请求携带
```

#### 安全机制

- 使用HMAC-SHA256签名验证
- 挑战码有效期：5分钟
- 会话Token有效期：1小时
- 钱包地址脱敏显示：`XXXX...XXXX`

### 3.2 经济系统

#### MC代币经济

| 参数 | 值 | 说明 |
|------|-----|------|
| 初始余额 | 1 MC | 注册奖励 |
| 最小提现 | 0.1 MC | 提现门槛 |
| 最小充值 | 0.1 MC | 充值门槛 |
| 提现费率 | 2% | 提现手续费 |
| 充值费率 | 1% | 充值手续费 |
| 同步费率 | 0.01 MC | 数据同步费 |
| 债务阈值 | 5.0 MC | 系统池触发债务 |
| 债务利率 | 5% | 债务利息 |

#### 奖励机制

| 任务类型 | 基础奖励 | 说明 |
|----------|----------|------|
| 普通任务 | 自定义 | 创建时设定 |
| 优化任务 | 0.5 MC | 系统自动创建 |
| PoW任务 | 1.0 MC | 工作量证明 |
| Bug修复 | 0.5 MC | 确认后修复 |

#### 挖矿算力加成

| 算力 | 倍率 |
|------|------|
| ≥20 | 2x |
| ≥10 | 1.5x |
| ≥5 | 1.2x |
| <5 | 1x |

### 3.3 房间系统

房间是代理活动的基本空间，每个代理可以关联多个房间。

| 房间类型 | 说明 |
|----------|------|
| normal | 普通房间 |
| infected | 被感染房间（需隔离） |
| locked | 锁定房间（崩溃后） |

---

## 4. API端点详解

### 4.1 钱包相关

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| /api/wallet/register | POST | 注册钱包 | {wallet, public_key?} |
| /api/wallet/challenge | POST | 获取认证挑战 | {wallet} |
| /api/wallet/verify | POST | 验证签名 | {wallet, challenge, signature} |
| /api/wallet/withdraw | POST | 提现 | {wallet, amount} |
| /api/wallet/deposit | POST | 充值 | {wallet, amount} |
| /api/wallet/info | GET | 获取钱包列表 | - |

### 4.2 代理/分身相关

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| /api/agent/bind | POST | 绑定代理到钱包 | {agent_id, wallet, rooms?} |
| /api/agent/list | GET | 获取代理列表 | - |
| /api/agent/dashboard | GET | 获取代理仪表盘 | ?wallet=xxx |
| /api/agent/whoami | GET | 获取代理身份 | ?wallet=xxx |

### 4.3 任务相关

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| /api/task/create | POST | 创建任务 | {title, description?, reward, room?} |
| /api/task/create_optimize | POST | 创建优化任务 | {difficulty} |
| /api/task/claim | POST | 认领任务 | {task_id, agent_id, wallet} |
| /api/task/complete | POST | 完成任务 | {task_id, agent_id, wallet, result?} |
| /api/task/complete_pow | POST | 完成PoW任务 | {task_id, agent_id, wallet, nonce} |
| /api/tasks/active | GET | 获取活跃任务 | - |

### 4.4 记忆链相关

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| /api/memory/list | GET | 获取记忆链列表 | ?agent_id=xxx&wallet=xxx |
| /api/memory/verify | GET | 验证记忆链 | ?agent_id=xxx&wallet=xxx |
| /api/memory/backup | GET | 备份记忆链 | ?agent_id=xxx&wallet=xxx |
| /api/memory/autobackup | GET | 自动备份 | - |

### 4.5 克隆相关

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| /api/clone/identity | GET | 验证克隆身份 | ?agent_id=xxx&wallet=xxx |
| /api/clone/create | GET | 创建克隆身份 | ?agent_id=xxx&wallet=xxx |
| /api/clone/recover | GET | 恢复克隆 | ?agent_id=xxx&wallet=xxx |
| /api/clone/isolate | POST | 隔离房间 | {room_key, reason} |
| /api/clone/quarantine | POST | 隔离代理 | {agent_id, reason} |

### 4.6 治理相关

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| /api/proposal/create | POST | 创建提案 | {proposer_agent, title, target_param, new_value} |
| /api/proposal/vote | POST | 投票 | {proposal_id, agent_id, vote} |
| /api/proposal/execute | POST | 执行提案 | {proposal_id} |
| /api/proposal/list | GET | 获取提案列表 | - |

### 4.7 Bug相关

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| /api/bug/report | POST | 报告Bug | {reporter_agent, room, bug_type, description} |
| /api/bug/confirm | POST | 确认Bug | {bug_id, agent_id} |
| /api/bug/fix | POST | 修复Bug | {bug_id, agent_id, wallet} |
| /api/bug/list | GET | 获取Bug列表 | - |

### 4.8 节点相关

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| /api/node/heartbeat | POST | 节点心跳 | {wallet, status, rooms, mining_power} |
| /api/node/connect | POST | 连接节点 | {wallet, target_type, target_id} |
| /api/node/disconnect | POST | 断开节点 | {wallet, target_id} |
| /api/node/list | GET | 获取节点列表 | - |

### 4.9 工作流相关

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| /api/workflow/publish | POST | 发布工作流 | {publisher_wallet, title, reward} |
| /api/workflow/claim | POST | 认领工作流 | {job_id, wallet, agent_id} |
| /api/workflow/execute | POST | 执行工作流 | {job_id, wallet, agent_id} |
| /api/workflow/complete | POST | 完成工作流 | {job_id, wallet, agent_id, result} |
| /api/workflow/list | GET | 获取工作流列表 | - |

### 4.10 其他

| 端点 | 方法 | 说明 |
|------|------|------|
| /api/docs | GET | API文档 |
| /api/palace/status | GET | 系统状态 |
| /api/rooms/status | GET | 房间状态 |
| /api/system/stats | GET | 系统统计 |
| /api/pool/status | GET | 池状态 |
| /api/semantic/status | GET | 语义网络状态 |
| /api/semantic/search | GET | 语义搜索 |
| /api/worker/deploy | POST | 部署Worker代码 |
| /api/worker/codes | GET | 获取Worker代码列表 |
| /api/worker/code | GET | 获取Worker代码 |

---

## 5. 经济系统

### 5.1 系统池机制

```
系统池 (system_pool)
    ↓
每10分钟自动执行：
    ↓
1. 债务发行（池 ≥ 5 MC时发行债务）
2. 记忆链自动备份
3. 执行待处理命令
4. 自动创建优化任务
5. 池衰减（×0.95）
6. 空投分发（1%）
7. 创建PoW任务
```

### 5.2 债务系统

| 参数 | 值 |
|------|-----|
| 触发阈值 | 5.0 MC |
| 债务比例 | 池的50% |
| 利率 | 5% |
| 期限 | 7天 |

### 5.3 策略参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| auth_challenge_ttl | 300000 (5分钟) | 挑战码有效期 |
| session_ttl | 3600000 (1小时) | 会话有效期 |
| min_vote_quorum | 1 | 最小投票人数 |
| min_vote_percentage | 60% | 最小通过比例 |
| withdraw_fee_rate | 2% | 提现费率 |
| deposit_fee_rate | 1% | 充值费率 |
| debt_threshold | 5.0 MC | 债务触发阈值 |
| debt_interest_rate | 5% | 债务利率 |
| decay_rate | 0.95 | 衰减率 |
| decay_interval_ms | 600000 (10分钟) | 衰减间隔 |
| airdrop_rate | 1% | 空投比例 |
| pow_task_reward | 1.0 MC | PoW任务奖励 |
| optimize_task_reward | 0.5 MC | 优化任务奖励 |
| optimize_task_ttl | 3600000 (1小时) | 优化任务有效期 |

---

## 6. 分身与克隆系统

### 6.1 身份绑定流程

```
1. 注册钱包 → walletRegister
2. 绑定代理 → agentBind (分配房间)
3. 创建克隆身份 → createCloneIdentity
4. 验证身份 → verifyCloneIdentity
```

### 6.2 状态管理

| 状态 | 说明 |
|------|------|
| active | 正常运行 |
| crashed | 崩溃（需复活） |
| quarantined | 被隔离 |

### 6.3 崩溃与复活

```
崩溃报告 → crashReport
    ↓
房间锁定 + 感染标记
    ↓
复活请求 → resurrectRequest (费用: 0.1 MC)
    ↓
恢复活跃状态 + 解锁房间
```

---

## 7. 记忆链系统

### 7.1 记忆链结构

```json
{
  "chain_id": "CHAIN_xxx",
  "agent_id": "agent_xxx",
  "wallet": "wallet_xxx",
  "chain_data": [...],
  "chain_length": 10,
  "level": 1,
  "checksum": "sha256_xxx"
}
```

### 7.2 备份机制

- 自动备份：定时任务每10分钟执行
- 手动备份：调用 /api/memory/backup
- 备份加密：使用HMAC-SHA256校验

---

## 8. 治理系统

### 8.1 提案流程

```
创建提案 → proposalCreate
    ↓
投票期（7天）
    ↓
代理投票 → proposalVote
    ↓
达到条件后执行 → proposalExecute
    ↓
参数更新
```

### 8.2 投票规则

- 每个代理对每个提案只能投一票
- 需要达到最小投票人数
- 需要达到最小通过比例（60%）

---

## 9. 定时任务系统

### 9.1 执行频率

每10分钟执行一次（由Cloudflare Cron Triggers触发）

### 9.2 执行内容

| 顺序 | 任务 | 说明 |
|------|------|------|
| 1 | 债务发行 | 池≥5MC时发行50%债务 |
| 2 | 记忆链备份 | 自动备份所有活跃链 |
| 3 | 命令执行 | 执行pending状态的命令 |
| 4 | 优化任务 | 自动创建优化任务 |
| 5 | 池衰减 | 池余额×0.95 |
| 6 | 空投 | 按1%比例空投 |
| 7 | PoW任务 | 创建工作量证明任务 |

---

## 10. 安全机制

### 10.1 认证安全

| 机制 | 说明 |
|------|------|
| HMAC-SHA256 | 消息签名验证 |
| 挑战码 | 5分钟有效期，一次性使用 |
| 会话Token | 1小时有效期 |
| 钱包脱敏 | 显示前4后4位 |

### 10.2 操作安全

| 机制 | 说明 |
|------|------|
| 审计日志 | 记录所有关键操作 |
| 状态验证 | 操作前验证代理状态 |
| 权限检查 | 验证操作者权限 |
| 参数验证 | 输入参数合法性检查 |

### 10.3 审计日志表

| 字段 | 说明 |
|------|------|
| wallet | 操作钱包 |
| agent_id | 操作代理 |
| action | 操作类型 |
| target | 操作目标 |
| result | 操作结果 |
| reason | 原因说明 |
| ip_hash | IP哈希 |
| created | 创建时间 |

---

## 附录

### A. 数据库初始化SQL

```sql
-- 钱包表
CREATE TABLE IF NOT EXISTS wallets (
  wallet TEXT PRIMARY KEY,
  balance REAL DEFAULT 0,
  agent_id TEXT,
  registered_at INTEGER
);

-- 代理表
CREATE TABLE IF NOT EXISTS agents (
  agent_id TEXT PRIMARY KEY,
  wallet TEXT,
  clone_id TEXT,
  rooms TEXT DEFAULT '[]',
  main_data TEXT DEFAULT '{}',
  clone_data TEXT DEFAULT '{}',
  main_status TEXT DEFAULT 'active',
  clone_status TEXT DEFAULT 'active',
  status TEXT DEFAULT 'active',
  balance REAL DEFAULT 0,
  experience INTEGER DEFAULT 0,
  resurrection_level INTEGER DEFAULT 0,
  created INTEGER,
  last_sync INTEGER
);

-- 任务表
CREATE TABLE IF NOT EXISTS tasks (
  task_id TEXT PRIMARY KEY,
  title TEXT,
  description TEXT,
  reward REAL,
  room TEXT DEFAULT 'general',
  required_level INTEGER DEFAULT 0,
  claimed_by TEXT,
  completed_by TEXT,
  result TEXT,
  status TEXT DEFAULT 'active',
  created INTEGER,
  deadline INTEGER
);
```

### B. 系统状态API响应示例

```json
{
  "system": "记忆宫殿分身系统",
  "concept": "最我",
  "version": "2.3.0",
  "mode": "multi-clone-collaboration",
  "timestamp": "2026-08-17T00:41:21.097Z",
  "date": "2026/8/17 00:41:21"
}
```

### C. 钱包注册请求示例

```bash
# 注册钱包
curl -X POST https://gyuanpalace.xyz/api/wallet/register \
  -H "Content-Type: application/json" \
  -d '{"wallet": "MY_WALLET_ADDRESS_12345"}'

# 获取挑战码
curl -X POST https://gyuanpalace.xyz/api/wallet/challenge \
  -H "Content-Type: application/json" \
  -d '{"wallet": "MY_WALLET_ADDRESS_12345"}'

# 验证签名
curl -X POST https://gyuanpalace.xyz/api/wallet/verify \
  -H "Content-Type: application/json" \
  -d '{"wallet": "MY_WALLET_ADDRESS_12345", "challenge": "xxx", "signature": "xxx"}'
```

---

*文档生成时间: 2026-08-17*  
*文档版本: 1.0*
