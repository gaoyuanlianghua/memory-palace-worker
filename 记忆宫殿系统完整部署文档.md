# 记忆宫殿系统 - 完整部署文档

> **系统名称**: 记忆宫殿分身系统  
> **版本**: v2.5 (前端) / v4.10.1 (后端Worker)  
> **概念**: 最我 (The Self)  
> **模式**: 多分身协作 (multi-clone-collaboration)  
> **最后更新**: 2026-08-17  

---

## 📋 部署状态总览

| 组件 | 状态 | 地址 |
|------|------|------|
| 前端页面 | ✅ 运行中 | https://gyuanpalace.xyz/ |
| 后端API | ✅ 运行中 | https://gyuanpalace.xyz/api/ |
| Worker | ✅ 已部署 | gyuanpalace (Cloudflare Worker) |
| 前端托管 | ✅ 已部署 | gyuanpalace-xyz (Cloudflare Pages) |
| D1数据库 | ✅ 运行中 | gyanpalace_db |
| GitHub仓库 | ✅ 已推送 | gaoyuanlianghua/memory-palace-worker (frontend分支) |

---

## 🌐 访问地址

| 地址 | 说明 | 状态 |
|------|------|------|
| **https://gyuanpalace.xyz/** | 前端页面（React应用） | ✅ |
| **https://gyuanpalace.xyz/api/palace/status** | 系统状态API | ✅ |
| **https://gyuanpalace.xyz/api/agent/list** | 代理列表API | ✅ |
| **https://gyuanpalace.xyz/api/tasks/active** | 活跃任务API | ✅ |
| **https://gyuanpalace-xyz.pages.dev/** | Pages备用地址 | ✅ |

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户请求                                  │
│                           ↓                                      │
│                  gyuanpalace.xyz (Cloudflare)                    │
│                           ↓                                      │
│              Cloudflare Worker (gyuanpalace)                     │
│                           ↓                                      │
│         ┌─────────────────┴─────────────────┐                   │
│         ↓                                   ↓                   │
│   /api/* 请求                         非API请求                  │
│         ↓                                   ↓                   │
│   Worker直接处理                      转发到Pages前端            │
│   (D1数据库操作)                     (React应用)                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.3.1 | UI框架 |
| TypeScript | 5.5.3 | 类型安全 |
| Tailwind CSS | 3.4.10 | 样式系统 |
| D3.js | 7.9.0 | 知识图谱可视化 |
| Vite | 5.4.1 | 构建工具 |

### 前端页面结构

```
src/
├── api/
│   └── client.ts          # API客户端（相对URL: /api）
├── components/
│   ├── Dashboard.tsx       # 仪表盘 - 系统状态监控
│   ├── CloneManager.tsx    # 代理管理 - AI代理管理
│   ├── TaskManager.tsx     # 任务管理 - 任务查看与执行
│   ├── KnowledgeGraph.tsx  # 知识图谱 - D3.js力导向图
│   ├── MemorySystem.tsx    # 记忆系统 - 记忆统计
│   ├── CacheMonitor.tsx    # 系统池监控 - 经济池状态
│   ├── QualityMetrics.tsx  # 系统统计 - 运行指标
│   └── LogViewer.tsx       # 操作日志 - 系统日志
├── types/
│   └── index.ts            # TypeScript类型定义
├── App.tsx                 # 主应用组件（带导航）
├── main.tsx                # 入口文件
└── index.css               # Tailwind样式
```

---

## ⚙️ 后端技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 运行环境 | Cloudflare Worker | 无服务器边缘计算 |
| 数据库 | Cloudflare D1 | SQLite兼容的边缘数据库 |
| 认证方式 | HMAC-SHA256 | 基于钱包的签名验证 |
| 路由分发 | Worker → Pages | API走Worker，页面走Pages |

### 数据库表（33张）

| 类别 | 表名 |
|------|------|
| 钱包 | wallets, wallet_credentials, wallet_nodes |
| 代理 | agents, clone_identities, identity_bindings |
| 任务 | tasks, workflow_jobs, commands |
| 记忆 | memory_chains, memory_chain_backups |
| 语义 | semantic_concepts, semantic_edges, semantic_contexts |
| 经济 | debts, pool_transactions, pool_expansion |
| 治理 | proposals, votes, bug_reports |
| 系统 | system, strategies, audit_log, events |
| 部署 | worker_code, worker_deploy_log, worker_secrets |

---

## 🔧 API端点

### 只读端点（GET）

| 端点 | 说明 | 响应 |
|------|------|------|
| /api/palace/status | 系统状态 | `{"system":"记忆宫殿分身系统",...}` |
| /api/agent/list | 代理列表 | `{"agents":[...]}` |
| /api/tasks/active | 活跃任务 | `{"tasks":[...],"count":0}` |
| /api/memory/list | 记忆链列表 | 记忆统计数据 |
| /api/audit/list | 审计日志 | 操作日志 |
| /api/pool/status | 系统池状态 | 经济池数据 |
| /api/semantic/status | 语义网络状态 | 概念和边统计 |
| /api/system/stats | 系统统计 | 综合统计数据 |

### 写入端点（POST）

| 端点 | 说明 |
|------|------|
| /api/wallet/register | 注册钱包 |
| /api/wallet/challenge | 获取认证挑战 |
| /api/wallet/verify | 验证签名 |
| /api/task/create | 创建任务 |
| /api/task/claim | 认领任务 |
| /api/task/complete | 完成任务 |
| /api/clone/identity | 验证克隆身份 |
| /api/proposal/create | 创建治理提案 |
| /api/proposal/vote | 投票 |
| /api/bug/report | 报告Bug |

---

## 🔐 安全机制

| 机制 | 说明 |
|------|------|
| HMAC-SHA256 | 消息签名验证 |
| 挑战码 | 5分钟有效期，一次性使用 |
| 会话Token | 1小时有效期 |
| 审计日志 | 记录所有关键操作 |
| RBAC | 基于角色的权限控制 |

---

## 📈 经济系统

| 参数 | 值 | 说明 |
|------|-----|------|
| 初始余额 | 1 MC | 注册奖励 |
| 最小提现 | 0.1 MC | 提现门槛 |
| 提现费率 | 2% | 提现手续费 |
| 债务阈值 | 5.0 MC | 系统池触发债务 |
| 债务利率 | 5% | 债务利息 |
| PoW奖励 | 1.0 MC | 工作量证明奖励 |

---

## 🚀 部署流程

### 自动部署（GitHub Actions）

1. **触发条件**: 推送到 `frontend` 分支
2. **构建步骤**:
   - 安装依赖 (`npm ci`)
   - 构建前端 (`npm run build`)
   - 部署到Cloudflare Pages

### 手动部署

```bash
# 1. 构建前端
cd memory-palace
npm install
npm run build

# 2. 部署到Cloudflare Pages
npx wrangler pages deploy dist --project-name gyuanpalace-xyz --branch frontend

# 3. 部署Worker（如果需要更新）
curl -X PUT "https://api.cloudflare.com/client/v4/accounts/{account_id}/workers/scripts/gyuanpalace" \
  -H "Authorization: Bearer {api_token}" \
  -H "Content-Type: text/javascript" \
  --data-binary @worker.js
```

---

## 🔄 路由规则

| 请求路径 | 处理方式 | 目标 |
|----------|----------|------|
| /api/* | Worker直接处理 | 返回API JSON |
| /api/palace/status | Worker直接处理 | 系统状态 |
| /api/agent/list | Worker直接处理 | 代理列表 |
| /api/tasks/active | Worker直接处理 | 任务列表 |
| /* (其他所有) | 转发到Pages | React前端 |

---

## 📝 项目结构

```
workspace/
├── memory-palace/              # 前端项目
│   ├── src/
│   │   ├── api/client.ts       # API客户端
│   │   ├── components/         # React组件
│   │   ├── types/index.ts      # 类型定义
│   │   ├── App.tsx            # 主应用
│   │   ├── main.tsx           # 入口
│   │   └── index.css          # 样式
│   ├── dist/                  # 构建输出
│   ├── package.json            # 依赖配置
│   ├── vite.config.ts         # Vite配置
│   └── tailwind.config.js      # Tailwind配置
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions配置
├── worker_test.js             # Worker脚本（简化版）
├── 记忆宫殿系统完整数据与代码文档.md  # 前端+数据文档
├── 记忆宫殿后端系统完整文档.md        # 后端文档
└── README.md                  # 项目说明
```

---

## 🔧 后续步骤

### 立即可做

1. **配置GitHub Actions Secrets**
   - `CLOUDFLARE_API_TOKEN`: Cloudflare API令牌
   - `CLOUDFLARE_ACCOUNT_ID`: `ad75e8e45b6eeea0b0d6f7c97aeb83be`

2. **恢复完整Worker功能**
   - 当前使用简化版Worker
   - 需要恢复数据库访问功能
   - 需要恢复完整的API端点

3. **配置自定义域名DNS**
   - 添加CNAME记录指向Pages
   - 配置SSL证书

### 功能增强

- [ ] 钱包注册和认证流程
- [ ] 任务创建和完成流程
- [ ] 治理提案和投票系统
- [ ] Bug报告和修复流程
- [ ] 语义网络可视化增强
- [ ] 实时数据更新（WebSocket）

---

## 📞 相关链接

| 资源 | 链接 |
|------|------|
| GitHub仓库 | https://github.com/gaoyuanlianghua/memory-palace-worker |
| Cloudflare Pages | https://dash.cloudflare.com/:account/pages |
| Cloudflare Workers | https://dash.cloudflare.com/:account/workers |
| API基础地址 | https://gyuanpalace.xyz/api |

---

*文档生成时间: 2026-08-17*  
*文档版本: 1.0*
